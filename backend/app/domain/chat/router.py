"""
Chat domain — FastAPI router stub.

Endpoints will include:
- POST /projects/{project_id}/sessions — create session
- GET  /projects/{project_id}/sessions — list sessions
- POST /projects/{project_id}/sessions/{session_id}/messages — send message (SSE stream)
"""

from fastapi import APIRouter

router = APIRouter()
# TODO: implement in Phase 3/4
