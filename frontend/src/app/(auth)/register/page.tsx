import { RegisterForm } from "@/components/auth/register-form";

export const metadata = {
  title: "Create Account — NL-DB Query Platform",
  description: "Register a new account on NL-DB Query Platform",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
